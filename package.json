{
  "name": "workers-asset-cache",
  "version": "2.0.0",
  "description": "Cloudflare Workers script to proxy and cache static content from external sources on Workers KV.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Max Ratmeyer"
}
